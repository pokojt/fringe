<div class="postItem">
  <div class="postDetailImage" style="background: url('{{post.image}}');">
  </div>
  <div class="postDetailContent">
    <h4>{{post.category.name}}</h4>
    <h2>{{post.title}}</h2>
    <h3>{{post.author}} | {{post.date}}</h3>
    <p>{{post.content}}</p>
  </div>
</div>
