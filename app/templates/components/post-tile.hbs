<div class="postItem">
  {{#link-to 'post' post.id}}<div class="postImage" style="background-image: url('{{post.image}}');">
  </div>{{/link-to}}
  <h4>{{post.category.name}}</h4>
  <h2>{{post.title}}</h2>
  <h3>{{post.author}} | {{post.date}}</h3>
  <p>{{post.summary}}</p>
  <hr>
</div>
