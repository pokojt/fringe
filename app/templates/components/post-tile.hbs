<div class="postItem">
  <div class="postImage" style="background-image: url('{{post.image}}');">
  </div>
  <h4>{{post.category}}</h4>
  <h2>{{post.title}}</h2>
  <h3>{{post.author}} | {{post.date}}</h3>
  <p>{{post.content}}</p>
</div>
